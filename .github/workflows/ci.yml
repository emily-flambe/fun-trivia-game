name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test

  worker-tests:
    name: Worker Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: mkdir -p dist && touch dist/.gitkeep
      - run: npm run test:worker

  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 1

  e2e-tests:
    name: E2E Tests
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Apply migrations to local D1
        run: |
          npx wrangler d1 execute trivia-trainer --local --file=migrations/0004_redesign.sql
          npx wrangler d1 execute trivia-trainer --local --file=migrations/0005_users.sql
          npx wrangler d1 execute trivia-trainer --local --file=migrations/0006_retry_tracking.sql

      - name: Seed local D1
        run: node scripts/seed.mjs --local

      - name: Create .dev.vars for local auth bypass
        run: echo 'CF_ACCESS_TEST_EMAIL=test@trivia.emilycogsdill.com' > .dev.vars

      - name: Run E2E tests
        run: npx playwright test

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
